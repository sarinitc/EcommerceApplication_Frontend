"use client";
import { inputClass, type FormData } from "./form-types";
export function ContactInfoCard({ data, onChange }: { data: FormData; onChange: (key: keyof FormData, value: FormData[keyof FormData]) => void }) { const fields = [['gender','Gender',['Male','Female','Other','Prefer not to say']],['language','Preferred Language',['English','French','Spanish','Japanese']],['currency','Preferred Currency',['USD','EUR','GBP','KHR']]] as const; return <section className="rounded-xl border border-slate-100 bg-white p-6 shadow-sm"><h2 className="text-base font-bold text-slate-800">Contact preferences</h2><div className="mt-5 grid grid-cols-2 gap-4">{fields.map(([key,label,options]) => <label key={key}><span className="mb-1 block text-sm font-medium text-gray-700">{label}</span><select value={data[key]} onChange={(e) => onChange(key,e.target.value)} className={inputClass}><option value="">Select {label}</option>{options.map((option) => <option key={option}>{option}</option>)}</select></label>)}<label><span className="mb-1 block text-sm font-medium text-gray-700">Date of Birth</span><input type="date" value={data.dateOfBirth} onChange={(e) => onChange('dateOfBirth',e.target.value)} className={inputClass} /></label></div></section>; }
