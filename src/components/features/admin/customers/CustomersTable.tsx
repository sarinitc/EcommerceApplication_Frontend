"use client";
import { ChevronDown, UsersRound } from "lucide-react";
import { CustomerRow } from "./CustomerRow";
import type { Customer } from "./customer-data";
const headings = ["Customer", "Phone", "Location", "Orders", "Total Spent", "Last Order", "Status"];
export function CustomersTable({ customers, selected, onToggle, onToggleAll, onOpen, onClear }: { customers: Customer[]; selected: Set<number>; onToggle: (id: number) => void; onToggleAll: () => void; onOpen: (customer: Customer) => void; onClear: () => void }) { const all = customers.length > 0 && customers.every((customer) => selected.has(customer.id)); return <section className="overflow-hidden rounded-xl border border-slate-100 bg-white shadow-sm">{customers.length ? <div className="overflow-x-auto"><table className="w-full min-w-[1170px]"><thead className="bg-slate-50/80 text-left text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th className="w-12 px-5 py-4"><input checked={all} onChange={onToggleAll} type="checkbox" className="h-4 w-4 rounded border-slate-300 text-indigo-600 focus:ring-indigo-500" /></th>{headings.map((heading, index) => <th key={heading} className={`px-4 py-4 ${index === 3 ? "text-center" : index === 4 ? "text-right" : ""}`}><span className="inline-flex items-center gap-1">{heading}<ChevronDown className="h-3.5 w-3.5" /></span></th>)}<th className="px-5 py-4 text-right">Actions</th></tr></thead><tbody>{customers.map((customer) => <CustomerRow key={customer.id} customer={customer} checked={selected.has(customer.id)} onCheck={() => onToggle(customer.id)} onOpen={() => onOpen(customer)} />)}</tbody></table></div> : <div className="grid min-h-80 place-items-center p-10 text-center"><div><span className="mx-auto grid h-12 w-12 place-items-center rounded-full bg-indigo-50 text-indigo-500"><UsersRound className="h-6 w-6" /></span><h2 className="mt-4 font-semibold text-slate-800">No customers found</h2><p className="mt-1 text-sm text-slate-500">Try changing your search or filters.</p><button onClick={onClear} className="mt-4 text-sm font-semibold text-indigo-600 hover:text-indigo-700">Clear filters</button></div></div>}</section>; }
