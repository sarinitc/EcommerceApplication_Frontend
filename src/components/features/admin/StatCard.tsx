import { ArrowDownRight, ArrowUpRight, type LucideIcon } from "lucide-react";

type StatCardProps = { icon: LucideIcon; iconBg: string; label: string; value: string; subtitle?: string; change?: string; trend?: "up" | "down" };

export function StatCard({ icon: Icon, iconBg, label, value, subtitle, change, trend }: StatCardProps) {
  const isUp = trend === "up";
  return <article className="group relative min-h-40 overflow-hidden rounded-2xl border border-slate-200/90 bg-white p-5 shadow-sm transition duration-200 hover:-translate-y-px hover:border-slate-300 hover:shadow-md"><div className={`absolute -right-3 -top-3 opacity-[0.08] transition duration-200 group-hover:scale-110 group-hover:opacity-[0.12] ${iconBg}`}><Icon className="h-28 w-28" strokeWidth={1.2} aria-hidden="true" /></div><div className={`relative grid h-10 w-10 place-items-center rounded-xl ${iconBg}`}><Icon className="h-5 w-5" strokeWidth={1.8} aria-hidden="true" /></div><p className="relative mt-4 text-sm font-semibold text-slate-700">{label}</p><p className="relative mt-1 text-3xl font-bold tracking-tight text-slate-950">{value}</p>{subtitle && <p className="relative mt-1 text-sm text-slate-500">{subtitle}</p>}{change && <p className="relative mt-3 flex items-center gap-1 text-xs"><span className={`inline-flex items-center rounded-full px-1.5 py-0.5 font-semibold ${isUp ? "bg-emerald-50 text-emerald-600" : "bg-red-50 text-red-500"}`}>{isUp ? <ArrowUpRight className="h-3.5 w-3.5" /> : <ArrowDownRight className="h-3.5 w-3.5" />}{change}</span><span className="text-slate-400">this month</span></p>}</article>;
}
