import Link from "next/link";
import type { RecentOrder } from "@/src/lib/dashboard";

const badge: Record<string, string> = { PENDING: "bg-amber-50 text-amber-700", PROCESSING: "bg-blue-50 text-blue-700", SHIPPED: "bg-indigo-50 text-indigo-700", DELIVERED: "bg-emerald-50 text-emerald-700", CANCELLED: "bg-red-50 text-red-700" };
const currency = new Intl.NumberFormat("en-US", { style: "currency", currency: "USD" });

export function RecentOrdersTable({ orders }: { orders: RecentOrder[] }) { return <section className="rounded-xl border border-gray-100 bg-white p-6 shadow-sm"><div className="mb-5 flex items-center justify-between"><h2 className="text-base font-bold text-slate-800">Recent Orders</h2><Link href="/admin/orders" className="text-sm font-semibold text-indigo-500 hover:text-indigo-700">View all orders</Link></div><table className="w-full text-left text-sm"><thead className="border-y border-gray-100 bg-gray-50/70 text-xs font-semibold text-slate-400"><tr><th className="px-3 py-3">Order ID</th><th className="px-3 py-3">Customer</th><th className="px-3 py-3">Date</th><th className="px-3 py-3">Amount</th><th className="px-3 py-3">Status</th></tr></thead><tbody>{orders.map((order) => <tr className="border-b border-gray-100 last:border-0" key={order.orderId}><td className="px-3 py-3.5 font-semibold text-indigo-500">#ORD-{order.orderId}</td><td className="px-3 py-3.5"><span className="flex items-center gap-2.5 font-medium text-slate-700"><span className="grid h-7 w-7 place-items-center rounded-full bg-indigo-50 text-xs font-bold text-indigo-600">{order.customerName.slice(0, 1).toUpperCase()}</span><span>{order.customerName}<small className="block font-normal text-slate-400">{order.email}</small></span></span></td><td className="px-3 py-3.5 text-slate-500">{new Intl.DateTimeFormat("en-US", { dateStyle: "medium" }).format(new Date(order.orderDate))}</td><td className="px-3 py-3.5 font-semibold text-slate-700">{currency.format(order.total)}</td><td className="px-3 py-3.5"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${badge[order.status] ?? "bg-gray-100 text-gray-700"}`}>{order.status}</span></td></tr>)}{orders.length === 0 && <tr><td className="px-3 py-8 text-center text-slate-400" colSpan={5}>No orders yet.</td></tr>}</tbody></table></section>; }
