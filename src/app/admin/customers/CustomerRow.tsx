"use client";
import { MoreHorizontal } from "lucide-react";
import type { Customer } from "./customer-data";
export const StatusBadge = ({ status }: { status: Customer["status"] }) => <span className={`inline-flex rounded-full px-2.5 py-1 text-xs font-semibold ${status === "Active" ? "bg-green-100 text-green-700" : status === "Blocked" ? "bg-red-100 text-red-700" : "bg-gray-100 text-gray-600"}`}>{status}</span>;
export function CustomerRow({ customer, checked, onCheck, onOpen }: { customer: Customer; checked: boolean; onCheck: () => void; onOpen: () => void }) { const initial = customer.name.trim().charAt(0).toUpperCase() || "C"; return <tr onClick={onOpen} className="cursor-pointer border-t border-slate-100 text-sm text-slate-600 transition hover:bg-slate-50"><td className="px-5 py-4" onClick={(event) => event.stopPropagation()}><input checked={checked} onChange={onCheck} type="checkbox" className="h-4 w-4 rounded border-slate-300 text-indigo-600 focus:ring-indigo-500" /></td><td className="min-w-[260px] px-2 py-4"><div className="flex items-center gap-3">{customer.avatar ? <img src={customer.avatar} alt="" className="h-9 w-9 rounded-full object-cover" /> : <span className="grid h-9 w-9 place-items-center rounded-full bg-indigo-100 text-sm font-bold text-indigo-600">{initial}</span>}<span><strong className="block font-semibold text-slate-800">{customer.name}</strong><span className="block text-xs text-slate-400">{customer.email}</span></span></div></td><td className="px-4 py-4 whitespace-nowrap">{customer.phone}</td><td className="px-4 py-4 whitespace-nowrap">{customer.location}, {customer.country}</td><td className="px-4 py-4 text-center font-medium">{customer.orderCount}</td><td className="px-4 py-4 text-right font-semibold text-slate-700">${customer.totalSpent.toFixed(2)}</td><td className="px-4 py-4 whitespace-nowrap">{new Intl.DateTimeFormat("en-US", { month: "short", day: "numeric", year: "numeric" }).format(new Date(customer.lastOrderDate))}</td><td className="px-4 py-4"><StatusBadge status={customer.status} /></td><td className="px-5 py-4 text-right" onClick={(event) => event.stopPropagation()}><button onClick={onOpen} className="rounded-md p-1.5 text-slate-400 hover:bg-slate-100 hover:text-slate-700" aria-label={`View ${customer.name}`}><MoreHorizontal className="h-5 w-5" /></button></td></tr>; }
