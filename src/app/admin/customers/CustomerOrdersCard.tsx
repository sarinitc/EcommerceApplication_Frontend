import Link from "next/link";
import type { Customer } from "./customer-data";
export function CustomerOrdersCard({ customer }: { customer: Customer }) { const avg = customer.totalSpent / Math.max(customer.orderCount,1); const date = (value: string) => new Intl.DateTimeFormat('en-US',{month:'short',day:'numeric',year:'numeric'}).format(new Date(value)); return <section className="rounded-xl border border-slate-100 bg-white p-6 shadow-sm"><div className="flex items-center justify-between"><h2 className="text-base font-bold text-slate-800">Order history</h2><Link href="/admin/orders" className="text-sm font-semibold text-indigo-600">View all orders</Link></div><div className="mt-5 grid grid-cols-4 gap-3">{[['Total Orders',customer.orderCount],['Total Spent',`$${customer.totalSpent.toFixed(2)}`],['Avg Order Value',`$${avg.toFixed(2)}`],['Last Order',date(customer.lastOrderDate)]].map(([label,value]) => <div key={String(label)} className="rounded-lg bg-slate-50 p-3"><p className="text-xs text-slate-400">{label}</p><p className="mt-1 font-bold text-slate-800">{value}</p></div>)}</div><table className="mt-5 w-full text-sm"><thead className="border-b text-left text-xs uppercase text-slate-400"><tr><th className="pb-3">Order ID</th><th>Date</th><th>Amount</th><th>Status</th></tr></thead><tbody>{customer.orders.slice(0,5).map((order) => <tr key={order.id} className="border-b border-slate-100 last:border-0"><td className="py-3 font-medium text-indigo-600">{order.id}</td><td>{date(order.date)}</td><td>${order.amount.toFixed(2)}</td><td><span className="rounded-full bg-green-100 px-2 py-1 text-xs font-medium text-green-700">{order.status}</span></td></tr>)}</tbody></table></section>; }
