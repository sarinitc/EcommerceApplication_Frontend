"use client";
import { ChevronLeft, ChevronRight } from "lucide-react";
export function Pagination({ page, pageSize, total, onPage, onPageSize }: { page: number; pageSize: number; total: number; onPage: (value: number) => void; onPageSize: (value: number) => void }) { const pages = Math.max(1, Math.ceil(total / pageSize)); const start = total ? page * pageSize + 1 : 0; const end = Math.min((page + 1) * pageSize, total); return <div className="flex items-center justify-between py-1"><p className="text-sm text-slate-500">Showing {start}-{end} of {total} customers</p><div className="flex items-center gap-2"><label className="mr-3 text-sm text-slate-500">Rows per page <select value={pageSize} onChange={(event) => onPageSize(Number(event.target.value))} className="ml-1 rounded-md border border-slate-200 bg-white px-2 py-1.5 text-slate-700 outline-none"><option>10</option><option>20</option><option>50</option><option>100</option></select></label><button disabled={page === 0} onClick={() => onPage(page - 1)} className="rounded-lg border border-slate-200 p-2 text-slate-500 disabled:opacity-40"><ChevronLeft className="h-4 w-4" /></button>{Array.from({ length: pages }, (_, index) => index).slice(0, 6).map((number) => <button key={number} onClick={() => onPage(number)} className={`grid h-9 w-9 place-items-center rounded-lg text-sm font-semibold ${number === page ? "bg-indigo-600 text-white" : "text-slate-600 hover:bg-indigo-50"}`}>{number + 1}</button>)}<button disabled={page === pages - 1} onClick={() => onPage(page + 1)} className="rounded-lg border border-slate-200 p-2 text-slate-500 disabled:opacity-40"><ChevronRight className="h-4 w-4" /></button></div></div>; }
