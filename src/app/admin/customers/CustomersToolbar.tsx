"use client";
import { Download, Mail, Search, Trash2 } from "lucide-react";
import { FilterDropdown } from "./FilterDropdown";
export type Filters = { status: string; location: string; type: string; sort: string };
export function CustomersToolbar({ search, setSearch, filters, setFilters, selectedCount, locations, onClear }: { search: string; setSearch: (value: string) => void; filters: Filters; setFilters: (filters: Filters) => void; selectedCount: number; locations: string[]; onClear: () => void }) { const set = (key: keyof Filters) => (value: string) => setFilters({ ...filters, [key]: value }); return <section className="rounded-xl border border-slate-100 bg-white p-4 shadow-sm"><div className="flex items-center gap-3"><label className="flex h-10 w-[340px] items-center gap-2 rounded-lg border border-slate-200 bg-slate-50 px-3 text-slate-400 focus-within:border-indigo-400 focus-within:bg-white"><Search className="h-4 w-4" /><input value={search} onChange={(event) => setSearch(event.target.value)} className="w-full bg-transparent text-sm text-slate-700 outline-none placeholder:text-slate-400" placeholder="Search by name, email, or phone..." /></label><FilterDropdown label="Status" value={filters.status} onChange={set("status")} options={["Active", "Inactive", "Blocked"]} /><FilterDropdown label="Location" value={filters.location} onChange={set("location")} options={locations} /><FilterDropdown label="Customer type" value={filters.type} onChange={set("type")} options={["New", "Returning"]} /><FilterDropdown label="Sort by" value={filters.sort} onChange={set("sort")} options={["Newest", "Highest Spend", "Most Orders", "Name A-Z"]} /><button onClick={onClear} className="text-sm font-medium text-slate-500 hover:text-indigo-600">Clear</button></div>{selectedCount > 0 && <div className="mt-4 flex items-center gap-3 border-t border-slate-100 pt-4"><strong className="text-sm text-slate-700">{selectedCount} selected</strong><button className="inline-flex items-center gap-1.5 text-sm font-medium text-indigo-600"><Download className="h-4 w-4" />Export Selected</button><button className="inline-flex items-center gap-1.5 text-sm font-medium text-slate-600"><Mail className="h-4 w-4" />Send Email</button><button onClick={() => window.dispatchEvent(new Event("admin-customers:delete-selected"))} className="inline-flex items-center gap-1.5 text-sm font-medium text-red-600 hover:text-red-700"><Trash2 className="h-4 w-4" />Delete</button></div>}</section>; }
