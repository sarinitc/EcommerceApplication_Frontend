"use client";
import { useState } from "react";
import { inputClass, type FormData } from "./form-types";
export function NotesCard({ data, onChange }: { data: FormData; onChange: (key: keyof FormData, value: FormData[keyof FormData]) => void }) { const [tag, setTag] = useState(''); const add = () => { const value = tag.trim(); if (value && !data.tags.includes(value)) onChange('tags',[...data.tags,value]); setTag(''); }; return <section className="rounded-xl border border-slate-100 bg-white p-5 shadow-sm"><h2 className="font-bold text-slate-800">Internal notes</h2><textarea value={data.notes} onChange={(e) => onChange('notes',e.target.value)} className={`${inputClass} mt-4 min-h-28 resize-y`} placeholder="Admin-only notes about this customer..." /><label className="mt-4 block"><span className="mb-1 block text-sm font-medium text-gray-700">Customer tags</span><div className="flex flex-wrap gap-2 rounded-md border border-gray-200 p-2 focus-within:ring-2 focus-within:ring-indigo-500">{data.tags.map((item) => <button type="button" key={item} onClick={() => onChange('tags',data.tags.filter((value) => value !== item))} className="rounded-full bg-indigo-50 px-2 py-1 text-xs font-semibold text-indigo-600">{item} ×</button>)}<input value={tag} onChange={(e) => setTag(e.target.value)} onKeyDown={(e) => { if (e.key === 'Enter' || e.key === ',') { e.preventDefault(); add(); } }} onBlur={add} className="min-w-24 flex-1 px-1 text-sm outline-none" placeholder="Add tag" /></div></label></section>; }
