"use client";
import { WandSparkles } from "lucide-react";
import { Toggle } from "./StatusCard";
import { inputClass, type FormData } from "./form-types";
export function AccountSettingsCard({ data, mode, onChange }: { data: FormData; mode: 'create' | 'edit'; onChange: (key: keyof FormData, value: FormData[keyof FormData]) => void }) { const generate = () => onChange('password', `Store!${crypto.randomUUID().slice(0,8)}`); return <section className="rounded-xl border border-slate-100 bg-white p-5 shadow-sm"><h2 className="font-bold text-slate-800">Account settings</h2><div className="mt-4 grid gap-4">{mode === 'create' && <Toggle label="Send account invite email" checked={data.sendInvite} onChange={(value) => onChange('sendInvite',value)} />}<Toggle label="Email verified" checked={data.emailVerified} onChange={(value) => onChange('emailVerified',value)} /><Toggle label="Allow marketing emails" checked={data.marketing} onChange={(value) => onChange('marketing',value)} />{mode === 'create' && <label><span className="mb-1 block text-sm font-medium text-gray-700">Temporary password</span><div className="flex gap-2"><input type="text" value={data.password} onChange={(e) => onChange('password',e.target.value)} className={inputClass} /><button type="button" onClick={generate} className="inline-flex shrink-0 items-center gap-1 rounded-md border border-indigo-200 px-2.5 text-sm font-semibold text-indigo-600"><WandSparkles className="h-4 w-4" />Generate</button></div></label>}</div></section>; }
