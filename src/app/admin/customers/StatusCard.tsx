"use client";
import { inputClass, toggleClass, type FormData } from "./form-types";
export function StatusCard({ data, onChange }: { data: FormData; onChange: (key: keyof FormData, value: FormData[keyof FormData]) => void }) { return <section className="rounded-xl border border-slate-100 bg-white p-5 shadow-sm"><h2 className="font-bold text-slate-800">Status</h2><div className="mt-4 grid gap-4"><label><span className="mb-1 block text-sm font-medium text-gray-700">Account Status</span><select value={data.status} onChange={(e) => onChange('status',e.target.value)} className={inputClass}><option>Active</option><option>Inactive</option><option>Blocked</option></select></label><label><span className="mb-1 block text-sm font-medium text-gray-700">Customer Type</span><select value={data.customerType} onChange={(e) => onChange('customerType',e.target.value)} className={inputClass}><option>New</option><option>Regular</option><option>VIP</option><option>Wholesale</option></select></label><Toggle label="Tax exempt" checked={data.taxExempt} onChange={(value) => onChange('taxExempt',value)} /></div></section>; }
export function Toggle({ label, checked, onChange }: { label: string; checked: boolean; onChange: (value: boolean) => void }) { return <label className="flex cursor-pointer items-center justify-between gap-3 text-sm font-medium text-slate-700">{label}<button type="button" onClick={() => onChange(!checked)} className={toggleClass(checked)} aria-pressed={checked}><span className={`absolute top-1 h-4 w-4 rounded-full bg-white shadow transition ${checked ? 'left-6' : 'left-1'}`} /></button></label>; }
